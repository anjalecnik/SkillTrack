{
  "name": "skilltrack",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "test:e2e": "npx playwright test playwright-tests"
  },
  "workspaces": [
    "api",
    "web"
  ],
  "devDependencies": {
    "turbo": "^1.12.0"
  },
  "packageManager": "npm@10.5.0",
  "dependencies": {
    "@playwright/test": "^1.52.0",
    "pg": "^8.16.0"
  }
}
